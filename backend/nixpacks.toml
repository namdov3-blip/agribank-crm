[phases.setup]
nixPkgs = ["nodejs-20_x", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "npx prisma migrate deploy", "npx prisma db seed"]

[start]
cmd = "npm start"
